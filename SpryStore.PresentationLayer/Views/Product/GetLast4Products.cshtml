@model List<Product>

@{
    ViewData["Title"] = "GetLast4Products";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<h4>Son Eklenen 4 Ürün</h4>

<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Ürün Adı</th>
        <th>Ürün Fiyatı</th>
    </tr>
    @foreach (var item in Model)
    {
        count++;
        <tr>
            <td>@count</td>
            <td>@item.ProductName</td>
            <td>@item.Price</td>
        </tr>
    }
</table>